swagger: "2.0"
info:
  title: bills/bills.proto
  version: version not set
tags:
  - name: BillService
  - name: BillAdminService
  - name: OrdersAdminService
  - name: OrderService
  - name: TicketsService
  - name: TicketsAdminService
consumes:
  - application/json
produces:
  - application/json
paths:
  /api/v1/console/bills/{id}:cancel:
    post:
      operationId: BillAdminService_CancelBill
      responses:
        "200":
          description: A successful response.
          schema:
            $ref: '#/definitions/apiResponse'
        default:
          description: An unexpected error response.
          schema:
            $ref: '#/definitions/rpcStatus'
      parameters:
        - name: id
          in: path
          required: true
          type: string
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/BillAdminServiceCancelBillBody'
      tags:
        - BillAdminService
  /api/v1/console/events/{event}/orders:
    get:
      operationId: OrdersAdminService_GetOrdersByEvent
      responses:
        "200":
          description: A successful response.
          schema:
            $ref: '#/definitions/apiResponse'
        default:
          description: An unexpected error response.
          schema:
            $ref: '#/definitions/rpcStatus'
      parameters:
        - name: event
          in: path
          required: true
          type: string
        - name: page
          in: query
          required: false
          type: integer
          format: int64
      tags:
        - OrdersAdminService
  /api/v1/console/events/{event}/tickets:
    get:
      operationId: TicketsAdminService_GetEventTicketTypes
      responses:
        "200":
          description: A successful response.
          schema:
            $ref: '#/definitions/apiResponse'
        default:
          description: An unexpected error response.
          schema:
            $ref: '#/definitions/rpcStatus'
      parameters:
        - name: event
          in: path
          required: true
          type: string
      tags:
        - TicketsAdminService
    post:
      operationId: TicketsAdminService_AddTicketType
      responses:
        "200":
          description: A successful response.
          schema:
            $ref: '#/definitions/apiResponse'
        default:
          description: An unexpected error response.
          schema:
            $ref: '#/definitions/rpcStatus'
      parameters:
        - name: event
          in: path
          required: true
          type: string
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/TicketsAdminServiceAddTicketTypeBody'
      tags:
        - TicketsAdminService
  /api/v1/console/events/{event}/tickets/order:
    patch:
      operationId: TicketsAdminService_UpdateTicketTypesOrder
      responses:
        "200":
          description: A successful response.
          schema:
            $ref: '#/definitions/apiResponse'
        default:
          description: An unexpected error response.
          schema:
            $ref: '#/definitions/rpcStatus'
      parameters:
        - name: event
          in: path
          required: true
          type: string
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/TicketsAdminServiceUpdateTicketTypesOrderBody'
      tags:
        - TicketsAdminService
  /api/v1/console/orders/{orderId}/bills:
    get:
      operationId: BillAdminService_GetBillsByOrder
      responses:
        "200":
          description: A successful response.
          schema:
            $ref: '#/definitions/apiResponse'
        default:
          description: An unexpected error response.
          schema:
            $ref: '#/definitions/rpcStatus'
      parameters:
        - name: orderId
          in: path
          required: true
          type: string
      tags:
        - BillAdminService
  /api/v1/console/orders/{order}:
    get:
      operationId: OrdersAdminService_GetOrderByID
      responses:
        "200":
          description: A successful response.
          schema:
            $ref: '#/definitions/apiResponse'
        default:
          description: An unexpected error response.
          schema:
            $ref: '#/definitions/rpcStatus'
      parameters:
        - name: order
          in: path
          required: true
          type: string
      tags:
        - OrdersAdminService
  /api/v1/console/orders/{order}/bills:
    post:
      operationId: BillAdminService_CreateBill
      responses:
        "200":
          description: A successful response.
          schema:
            $ref: '#/definitions/apiResponse'
        default:
          description: An unexpected error response.
          schema:
            $ref: '#/definitions/rpcStatus'
      parameters:
        - name: order
          in: path
          required: true
          type: string
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/BillAdminServiceCreateBillBody'
      tags:
        - BillAdminService
  /api/v1/console/orders/{order}/items/{item}:
    patch:
      operationId: OrdersAdminService_ChangeOrderItemStatus
      responses:
        "200":
          description: A successful response.
          schema:
            $ref: '#/definitions/apiResponse'
        default:
          description: An unexpected error response.
          schema:
            $ref: '#/definitions/rpcStatus'
      parameters:
        - name: order
          in: path
          required: true
          type: string
        - name: item
          in: path
          required: true
          type: string
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/OrdersAdminServiceChangeOrderItemStatusBody'
      tags:
        - OrdersAdminService
  /api/v1/console/orders/{order}/items/{item}:reject-refund:
    post:
      operationId: OrdersAdminService_RejectOrderItemRefund
      responses:
        "200":
          description: A successful response.
          schema:
            $ref: '#/definitions/apiResponse'
        default:
          description: An unexpected error response.
          schema:
            $ref: '#/definitions/rpcStatus'
      parameters:
        - name: order
          in: path
          required: true
          type: string
        - name: item
          in: path
          required: true
          type: string
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/OrdersAdminServiceRejectOrderItemRefundBody'
      tags:
        - OrdersAdminService
  /api/v1/console/orders/{order}:cancel:
    post:
      operationId: OrdersAdminService_CancelOrder
      responses:
        "200":
          description: A successful response.
          schema:
            $ref: '#/definitions/apiResponse'
        default:
          description: An unexpected error response.
          schema:
            $ref: '#/definitions/rpcStatus'
      parameters:
        - name: order
          in: path
          required: true
          type: string
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/ordersOrdersAdminServiceCancelOrderBody'
      tags:
        - OrdersAdminService
  /api/v1/console/refunds:
    post:
      operationId: BillAdminService_CreateRefundBill
      responses:
        "200":
          description: A successful response.
          schema:
            $ref: '#/definitions/apiResponse'
        default:
          description: An unexpected error response.
          schema:
            $ref: '#/definitions/rpcStatus'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/billsCreateRefundBillRequest'
      tags:
        - BillAdminService
  /api/v1/console/sessions/{session}/orders:
    get:
      operationId: OrdersAdminService_GetOrdersBySession
      responses:
        "200":
          description: A successful response.
          schema:
            $ref: '#/definitions/apiResponse'
        default:
          description: An unexpected error response.
          schema:
            $ref: '#/definitions/rpcStatus'
      parameters:
        - name: session
          in: path
          required: true
          type: string
        - name: page
          in: query
          required: false
          type: integer
          format: int64
      tags:
        - OrdersAdminService
  /api/v1/console/tickets/{ticket}:
    delete:
      operationId: TicketsAdminService_DeleteTicketType
      responses:
        "200":
          description: A successful response.
          schema:
            $ref: '#/definitions/apiResponse'
        default:
          description: An unexpected error response.
          schema:
            $ref: '#/definitions/rpcStatus'
      parameters:
        - name: ticket
          in: path
          required: true
          type: string
      tags:
        - TicketsAdminService
    put:
      operationId: TicketsAdminService_UpdateTicketType
      responses:
        "200":
          description: A successful response.
          schema:
            $ref: '#/definitions/apiResponse'
        default:
          description: An unexpected error response.
          schema:
            $ref: '#/definitions/rpcStatus'
      parameters:
        - name: ticket
          in: path
          required: true
          type: string
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/TicketsAdminServiceUpdateTicketTypeBody'
      tags:
        - TicketsAdminService
  /api/v1/console/tickets:check-in:
    post:
      operationId: TicketsAdminService_CheckInTicket
      responses:
        "200":
          description: A successful response.
          schema:
            $ref: '#/definitions/apiResponse'
        default:
          description: An unexpected error response.
          schema:
            $ref: '#/definitions/rpcStatus'
      parameters:
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/ticketsCheckInTicketRequest'
      tags:
        - TicketsAdminService
  /api/v1/events/{event}/orders:
    post:
      operationId: OrderService_AddOrder
      responses:
        "200":
          description: A successful response.
          schema:
            $ref: '#/definitions/apiResponse'
        default:
          description: An unexpected error response.
          schema:
            $ref: '#/definitions/rpcStatus'
      parameters:
        - name: event
          in: path
          required: true
          type: string
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/OrderServiceAddOrderBody'
      tags:
        - OrderService
  /api/v1/events/{event}/tickets:
    get:
      operationId: TicketsService_GetEventTickets
      responses:
        "200":
          description: A successful response.
          schema:
            $ref: '#/definitions/apiResponse'
        default:
          description: An unexpected error response.
          schema:
            $ref: '#/definitions/rpcStatus'
      parameters:
        - name: event
          in: path
          required: true
          type: string
      tags:
        - TicketsService
  /api/v1/orders:
    get:
      operationId: OrderService_GetUserOrders
      responses:
        "200":
          description: A successful response.
          schema:
            $ref: '#/definitions/apiResponse'
        default:
          description: An unexpected error response.
          schema:
            $ref: '#/definitions/rpcStatus'
      parameters:
        - name: after
          in: query
          required: false
          type: string
      tags:
        - OrderService
  /api/v1/orders/{orderId}/transactions:
    get:
      operationId: BillService_GetTransactionHistory
      responses:
        "200":
          description: A successful response.
          schema:
            $ref: '#/definitions/apiResponse'
        default:
          description: An unexpected error response.
          schema:
            $ref: '#/definitions/rpcStatus'
      parameters:
        - name: orderId
          in: path
          required: true
          type: string
      tags:
        - BillService
  /api/v1/orders/{order}/items/{item}:request-refund:
    post:
      operationId: OrderService_RequestOrderItemRefund
      responses:
        "200":
          description: A successful response.
          schema:
            $ref: '#/definitions/apiResponse'
        default:
          description: An unexpected error response.
          schema:
            $ref: '#/definitions/rpcStatus'
      parameters:
        - name: order
          in: path
          required: true
          type: string
        - name: item
          in: path
          required: true
          type: string
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/OrderServiceRequestOrderItemRefundBody'
      tags:
        - OrderService
  /api/v1/orders/{order}:cancel:
    post:
      operationId: OrderService_CancelOrder
      responses:
        "200":
          description: A successful response.
          schema:
            $ref: '#/definitions/apiResponse'
        default:
          description: An unexpected error response.
          schema:
            $ref: '#/definitions/rpcStatus'
      parameters:
        - name: order
          in: path
          required: true
          type: string
        - name: body
          in: body
          required: true
          schema:
            $ref: '#/definitions/ordersOrderServiceCancelOrderBody'
      tags:
        - OrderService
  /api/v1/sessions/{session}/tickets:
    get:
      operationId: TicketsService_GetSessionTickets
      responses:
        "200":
          description: A successful response.
          schema:
            $ref: '#/definitions/apiResponse'
        default:
          description: An unexpected error response.
          schema:
            $ref: '#/definitions/rpcStatus'
      parameters:
        - name: session
          in: path
          required: true
          type: string
      tags:
        - TicketsService
definitions:
  BillAdminServiceCancelBillBody:
    type: object
    properties:
      reason:
        type: string
  BillAdminServiceCreateBillBody:
    type: object
    properties:
      amount:
        type: string
      paymentMethod:
        $ref: '#/definitions/apibillsPaymentMethod'
  ChangeOrderItemStatusRequestOrderItemStatus:
    type: string
    enum:
      - UNKNOWN
      - CANCELED
      - RETURN_REQUESTED
      - RETURN_APPROVED
    default: UNKNOWN
  OrderServiceAddOrderBody:
    type: object
    properties:
      items:
        type: array
        items:
          type: object
          $ref: '#/definitions/ordersAddOrderRequestItem'
      subtotal:
        type: string
      total:
        type: string
      merchantId:
        type: string
      paymentMethod:
        $ref: '#/definitions/ordersAddOrderRequestPaymentMethod'
  OrderServiceRequestOrderItemRefundBody:
    type: object
    properties:
      reason:
        type: string
  OrdersAdminServiceChangeOrderItemStatusBody:
    type: object
    properties:
      status:
        $ref: '#/definitions/ChangeOrderItemStatusRequestOrderItemStatus'
      reason:
        type: string
  OrdersAdminServiceRejectOrderItemRefundBody:
    type: object
    properties:
      reason:
        type: string
  TicketsAdminServiceAddTicketTypeBody:
    type: object
    properties:
      name:
        type: string
        title: |-
          repeated string session = 1 [(buf.validate.field).repeated = {min_items: 1,
             unique:true,
             items: {
               string: {
                 len: 24
               }
             }}];
      quantity:
        type: integer
        format: int64
      price:
        type: string
      desc:
        type: string
      sessions:
        type: array
        items:
          type: object
          $ref: '#/definitions/ticketsAddTicketTypeRequestSession'
      visibility:
        type: boolean
      startShowingOn:
        type: string
        format: date-time
      stopShowingOn:
        type: string
        format: date-time
      pick:
        type: integer
        format: int64
      minQuantityPerOrder:
        type: integer
        format: int64
      maxQuantityPerOrder:
        type: integer
        format: int64
      dueDuration:
        type: string
      enable:
        type: boolean
      saleTimeSetting:
        type: string
  TicketsAdminServiceUpdateTicketTypeBody:
    type: object
    properties:
      name:
        type: string
      quantity:
        type: integer
        format: int64
      price:
        type: string
      desc:
        type: string
      saleStartAt:
        type: string
        format: date-time
      saleEndAt:
        type: string
        format: date-time
      visibility:
        type: boolean
      startShowingOn:
        type: string
        format: date-time
      stopShowingOn:
        type: string
        format: date-time
      minQuantityPerOrder:
        type: integer
        format: int64
      maxQuantityPerOrder:
        type: integer
        format: int64
      dueDuration:
        type: string
      saleTimeSetting:
        type: string
      sessions:
        type: array
        items:
          type: object
          $ref: '#/definitions/ticketsUpdateTicketTypeRequestSession'
      enable:
        type: boolean
  TicketsAdminServiceUpdateTicketTypesOrderBody:
    type: object
    properties:
      order:
        type: array
        items:
          type: string
  apiResponse:
    type: object
    properties:
      status:
        type: string
      code:
        type: integer
        format: int32
      message:
        type: string
      data:
        $ref: '#/definitions/protobufAny'
  apibillsPaymentMethod:
    type: object
    properties:
      id:
        type: string
      name:
        type: string
  billsCreateRefundBillRequest:
    type: object
    properties:
      orderItemId:
        type: string
      amount:
        type: string
      paymentMethod:
        $ref: '#/definitions/apibillsPaymentMethod'
      reason:
        type: string
  ordersAddOrderRequestItem:
    type: object
    properties:
      ticket:
        type: string
      quantity:
        type: integer
        format: int64
  ordersAddOrderRequestPaymentMethod:
    type: object
    properties:
      id:
        type: string
      name:
        type: string
  ordersOrderServiceCancelOrderBody:
    type: object
    properties:
      reason:
        type: string
  ordersOrdersAdminServiceCancelOrderBody:
    type: object
    properties:
      reason:
        type: string
  protobufAny:
    type: object
    properties:
      '@type':
        type: string
        description: |-
          A URL/resource name that uniquely identifies the type of the serialized
          protocol buffer message. This string must contain at least
          one "/" character. The last segment of the URL's path must represent
          the fully qualified name of the type (as in
          `path/google.protobuf.Duration`). The name should be in a canonical form
          (e.g., leading "." is not accepted).

          In practice, teams usually precompile into the binary all types that they
          expect it to use in the context of Any. However, for URLs which use the
          scheme `http`, `https`, or no scheme, one can optionally set up a type
          server that maps type URLs to message definitions as follows:

          * If no scheme is provided, `https` is assumed.
          * An HTTP GET on the URL must yield a [google.protobuf.Type][]
            value in binary format, or produce an error.
          * Applications are allowed to cache lookup results based on the
            URL, or have them precompiled into a binary to avoid any
            lookup. Therefore, binary compatibility needs to be preserved
            on changes to types. (Use versioned type names to manage
            breaking changes.)

          Note: this functionality is not currently available in the official
          protobuf release, and it is not used for type URLs beginning with
          type.googleapis.com. As of May 2023, there are no widely used type server
          implementations and no plans to implement one.

          Schemes other than `http`, `https` (or the empty scheme) might be
          used with implementation specific semantics.
    additionalProperties: {}
    description: |-
      `Any` contains an arbitrary serialized protocol buffer message along with a
      URL that describes the type of the serialized message.

      Protobuf library provides support to pack/unpack Any values in the form
      of utility functions or additional generated methods of the Any type.

      Example 1: Pack and unpack a message in C++.

          Foo foo = ...;
          Any any;
          any.PackFrom(foo);
          ...
          if (any.UnpackTo(&foo)) {
            ...
          }

      Example 2: Pack and unpack a message in Java.

          Foo foo = ...;
          Any any = Any.pack(foo);
          ...
          if (any.is(Foo.class)) {
            foo = any.unpack(Foo.class);
          }
          // or ...
          if (any.isSameTypeAs(Foo.getDefaultInstance())) {
            foo = any.unpack(Foo.getDefaultInstance());
          }

       Example 3: Pack and unpack a message in Python.

          foo = Foo(...)
          any = Any()
          any.Pack(foo)
          ...
          if any.Is(Foo.DESCRIPTOR):
            any.Unpack(foo)
            ...

       Example 4: Pack and unpack a message in Go

           foo := &pb.Foo{...}
           any, err := anypb.New(foo)
           if err != nil {
             ...
           }
           ...
           foo := &pb.Foo{}
           if err := any.UnmarshalTo(foo); err != nil {
             ...
           }

      The pack methods provided by protobuf library will by default use
      'type.googleapis.com/full.type.name' as the type URL and the unpack
      methods only use the fully qualified type name after the last '/'
      in the type URL, for example "foo.bar.com/x/y.z" will yield type
      name "y.z".

      JSON
      ====
      The JSON representation of an `Any` value uses the regular
      representation of the deserialized, embedded message, with an
      additional field `@type` which contains the type URL. Example:

          package google.profile;
          message Person {
            string first_name = 1;
            string last_name = 2;
          }

          {
            "@type": "type.googleapis.com/google.profile.Person",
            "firstName": <string>,
            "lastName": <string>
          }

      If the embedded message type is well-known and has a custom JSON
      representation, that representation will be embedded adding a field
      `value` which holds the custom JSON in addition to the `@type`
      field. Example (for message [google.protobuf.Duration][]):

          {
            "@type": "type.googleapis.com/google.protobuf.Duration",
            "value": "1.212s"
          }
  rpcStatus:
    type: object
    properties:
      code:
        type: integer
        format: int32
      message:
        type: string
      details:
        type: array
        items:
          type: object
          $ref: '#/definitions/protobufAny'
  ticketsAddTicketTypeRequestSession:
    type: object
    properties:
      session:
        type: string
      saleStartAt:
        type: string
        format: date-time
      saleEndAt:
        type: string
        format: date-time
  ticketsCheckInTicketRequest:
    type: object
    properties:
      qrCode:
        type: string
  ticketsUpdateTicketTypeRequestSession:
    type: object
    properties:
      session:
        type: string
      saleStartAt:
        type: string
        format: date-time
      saleEndAt:
        type: string
        format: date-time
